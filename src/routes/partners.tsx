import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { ArrowLeft, ArrowRight, Building2, Handshake, Loader2, Package, Store, Truck } from "lucide-react";
import { toast } from "sonner";
import { Link } from "@tanstack/react-router";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";

export const Route=createFileRoute("/partners")({component:PartnersPage});
function PartnersPage(){const [user,setUser]=useState<any>(null);const [form,setForm]=useState({business_name:"",partner_type:"retailer",description:"",phone:"",city:"",website:""});const [busy,setBusy]=useState(false);const [submitted,setSubmitted]=useState(false);useEffect(()=>{void supabase.auth.getUser().then(({data})=>setUser(data.user))},[]);function update(k:string,v:string){setForm(f=>({...f,[k]:v}))}async function submit(){if(!user){toast.error("Sign in to create a partner profile.");return}if(!form.business_name.trim()){toast.error("Business name is required.");return}setBusy(true);const {error}=await (supabase as any).from("partner_profiles").insert({...form,owner_user_id:user.id});setBusy(false);if(error)toast.error(error.message);else{setSubmitted(true);toast.success("Partner profile submitted")}}return <main className="min-h-screen bg-surface text-foreground"><div className="mx-auto max-w-5xl px-4 py-8 sm:px-6 sm:py-16"><Link to="/" className="inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4"/>Back to Strap</Link><section className="mt-14 rounded-[2rem] border border-border bg-background p-7 shadow-sm sm:p-12"><div className="flex items-center gap-3"><span className="flex size-11 items-center justify-center rounded-2xl bg-accent-soft text-accent-ink"><Handshake className="size-5"/></span><div><p className="text-xs font-bold uppercase tracking-[.14em] text-accent-ink">Partner marketplace</p><h1 className="mt-1 font-display text-4xl font-bold tracking-tight sm:text-6xl">Find businesses ready to move products further.</h1></div></div><p className="mt-5 max-w-2xl text-lg leading-8 text-muted-foreground">Create a partner profile for producers, wholesalers, retailers or dropshippers. Approval keeps the marketplace trustworthy before matching begins.</p><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[[Building2,"Producers"],[Package,"Wholesalers"],[Store,"Retailers"],[Truck,"Dropshippers"]].map(([Icon,label])=><div key={label as string} className="rounded-2xl border border-border p-4"><Icon className="size-5 text-accent-ink"/><p className="mt-3 text-sm font-semibold">{label as string}</p></div>)}</div>{submitted?<div className="mt-10 rounded-2xl border border-accent/20 bg-accent-soft p-6"><p className="font-semibold">Profile submitted.</p><p className="mt-1 text-sm leading-6 text-muted-foreground">Your partner profile is pending review. You can return to Strap while marketplace matching is prepared.</p><Link to="/" className="mt-5 inline-flex items-center gap-2 rounded-xl bg-foreground px-4 py-2.5 text-sm font-semibold text-background">Back to Strap<ArrowRight className="size-4"/></Link></div>:<div className="mt-10 rounded-2xl border border-border p-5 sm:p-7"><h2 className="font-display text-2xl font-bold">Create partner profile</h2><p className="mt-1 text-sm text-muted-foreground">Use accurate business details so future matches are useful.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Business name<Input className="mt-2" value={form.business_name} onChange={e=>update("business_name",e.target.value)} placeholder="Your business"/></label><label className="text-sm font-medium">Partner type<select className="mt-2 h-10 w-full rounded-md border border-border bg-background px-3" value={form.partner_type} onChange={e=>update("partner_type",e.target.value)}><option value="producer">Producer</option><option value="wholesaler">Wholesaler</option><option value="retailer">Retailer</option><option value="dropshipper">Dropshipper</option></select></label><label className="text-sm font-medium">Phone<Input className="mt-2" value={form.phone} onChange={e=>update("phone",e.target.value)}/></label><label className="text-sm font-medium">City<Input className="mt-2" value={form.city} onChange={e=>update("city",e.target.value)}/></label><label className="text-sm font-medium sm:col-span-2">Website<Input className="mt-2" value={form.website} onChange={e=>update("website",e.target.value)} placeholder="https://"/></label><label className="text-sm font-medium sm:col-span-2">Description<Textarea className="mt-2" value={form.description} onChange={e=>update("description",e.target.value)} placeholder="What do you supply or sell?"/></label></div><Button className="mt-5" onClick={()=>void submit()} disabled={busy}>{busy?<Loader2 className="mr-2 size-4 animate-spin"/>:<Handshake className="mr-2 size-4"/>}{busy?"Submitting…":user?"Submit profile":"Sign in to continue"}</Button></div>}</section></div></main>}
