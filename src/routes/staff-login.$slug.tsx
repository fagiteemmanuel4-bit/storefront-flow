import { createFileRoute, Link } from "@tanstack/react-router";
import { KeyRound, ShieldCheck, UserRound } from "lucide-react";
import { useQuery } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
export const Route=createFileRoute("/staff-login/$slug")({component:StaffLoginPage});
type Profile={id:string;store_id:string;name:string;role:string;is_active:boolean;login_slug:string;store_name:string;primary_branch_id:string|null};
function StaffLoginPage(){const {slug}=Route.useParams();const q=useQuery({queryKey:["staff-login-profile",slug],queryFn:async()=>{const {data,error}=await(supabase as any).rpc("staff_login_profile",{_slug:slug});if(error)throw error;return(data&&data.id?data:null) as Profile|null;}});const p=q.data;function continueToStaff(){window.sessionStorage.setItem("kudi-staff-login-slug",slug);window.location.assign("/staff");}return <main className="min-h-dvh bg-background px-4 py-8"><div className="mx-auto flex min-h-[80dvh] max-w-md items-center"><section className="w-full rounded-[2rem] border border-border bg-surface p-6 shadow-lift sm:p-8"><div className="flex size-12 items-center justify-center rounded-2xl bg-accent-soft"><UserRound className="size-5 text-accent-ink"/></div>{q.isLoading?<><div className="mt-6 h-6 w-40 animate-pulse rounded bg-secondary"/><div className="mt-2 h-4 w-64 animate-pulse rounded bg-secondary"/></>:q.isError||!p?<><h1 className="mt-6 font-display text-2xl font-bold">Staff link unavailable</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">This staff login link is invalid or the account is no longer available.</p><Link to="/auth" className="mt-6 inline-flex"><Button>Go to Strap</Button></Link></>:<><p className="mt-6 text-label-caps text-muted-foreground">Staff sign-in</p><h1 className="mt-2 font-display text-3xl font-bold tracking-tight">Welcome, {p.name}</h1><p className="mt-2 text-sm text-muted-foreground">{p.store_name} · {p.role}</p><div className="mt-6 rounded-2xl border border-border bg-secondary/50 p-4"><div className="flex gap-3"><ShieldCheck className="mt-0.5 size-4 shrink-0"/><p className="text-xs leading-5 text-muted-foreground">This dedicated link opens the exact staff profile. The PIN remains private and is never embedded in the URL.</p></div></div><Button className="mt-5 h-12 w-full" onClick={continueToStaff}><KeyRound className="mr-2 size-4"/>Continue to sign in</Button></>}</section></div></main>}
