import { useMemo, useState } from "react";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
export function CalculatorExtension({ currency = "NGN" }: { currency?: string }) { const[cost,setCost]=useState(0);const[price,setPrice]=useState(0);const[discount,setDiscount]=useState(0);const result=useMemo(()=>{const discounted=price*Math.max(0,1-discount/100);const profit=discounted-cost;return{discounted,profit,margin:discounted?(profit/discounted)*100:0}},[cost,price,discount]);const money=(value:number)=>new Intl.NumberFormat("en-NG",{style:"currency",currency,maximumFractionDigits:0}).format(value);return <div className="space-y-4"><div className="grid gap-3 sm:grid-cols-3"><div><Label className="text-xs font-semibold">Cost</Label><Input className="mt-1.5" type="number" min="0" value={cost||""} onChange={e=>setCost(Number(e.target.value)||0)}/></div><div><Label className="text-xs font-semibold">Selling price</Label><Input className="mt-1.5" type="number" min="0" value={price||""} onChange={e=>setPrice(Number(e.target.value)||0)}/></div><div><Label className="text-xs font-semibold">Discount %</Label><Input className="mt-1.5" type="number" min="0" value={discount||""} onChange={e=>setDiscount(Number(e.target.value)||0)}/></div></div><div className="grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-secondary/60 p-4"><p className="text-xs text-muted-foreground">Customer pays</p><p className="mt-1 font-display text-lg font-bold">{money(result.discounted)}</p></div><div className="rounded-xl bg-secondary/60 p-4"><p className="text-xs text-muted-foreground">Profit</p><p className="mt-1 font-display text-lg font-bold">{money(result.profit)}</p></div><div className="rounded-xl bg-accent-soft p-4"><p className="text-xs text-muted-foreground">Margin</p><p className="mt-1 font-display text-lg font-bold text-accent-ink">{result.margin.toFixed(1)}%</p></div></div></div> }
